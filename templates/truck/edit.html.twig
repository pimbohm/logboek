{% extends 'base.html.twig' %}

{% block title %}Edit Truck{% endblock %}
{% block body %}
    {% if is_granted('ROLE_ADMIN')%}
    <h1>Edit Truck</h1>

    {{ include('truck/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('truck_index') }}">back to list</a>

    {{ include('truck/_delete_form.html.twig') }}
        {% else %}
            <h1> No access granted </h1>
    {% endif %}
{% endblock %}