{% extends 'base.html.twig' %}

{% block title %}New Truck{% endblock %}

{% block body %}
    {% if is_granted('ROLE_ADMIN')%}
    <h1>Create new Truck</h1>

    {{ include('truck/_form.html.twig') }}

    <a href="{{ path('truck_index') }}">back to list</a>
    {% else %}
    <h1>No access granted</h1>
    {% endif %}
{% endblock %}
